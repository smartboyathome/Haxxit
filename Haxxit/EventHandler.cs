using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace SmartboyDevelopments.Haxxit
{
    /*
     * Utilize the Notifiable/Observer pattern to create an alternative to the event-delegate framework found in C#.
     * This means registering to events based on type, and notifying based on that type (including inheritance). With
     * this structure, something could register to all events, or some of them, just by specifying a type.
     */
    class EventHandler
    {
    }


}